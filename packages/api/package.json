{
	"name": "api",
	"version": "0.0.0",
	"type": "module",
	"exports": {
		".": {
			"types": "./index.d.ts",
			"default": "./src/index.ts"
		},
		"./app": "./src/app.ts"
	},
	"scripts": {
		"dev": "wrangler dev --port 8788",
		"deploy": "wrangler deploy --minify",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "wrangler d1 migrations apply DB --local",
		"test": "vitest run",
		"test:watch": "vitest",
		"generate-types": "wrangler types",
		"build:types": "bunx tsc -p tsconfig.build.json",
		"typecheck": "bun run generate-types && bun run tsc"
	},
	"dependencies": {
		"hono": "^4.12.9",
		"drizzle-orm": "^0.45.1",
		"zod": "^4.3.6",
		"pino": "^10.3.1"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.14.1",
		"@types/bun": "^1.3.11",
		"@types/node": "^22",
		"drizzle-kit": "^0.31.10",
		"pino-pretty": "^13.1.3",
		"vitest": "^4.1.2",
		"wrangler": "^4.80.0"
	}
}
